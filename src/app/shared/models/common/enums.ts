export enum UserState {
    guest = "guest",
    authorized = "authorized",
}

export enum ManagerContext {
    login = "login", 
    start = "start", 
    base = "base",
    indexes = "indexes",
    corrections = "corrections"
}

