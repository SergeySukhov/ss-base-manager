export enum UserState {
    guest = "guest",
    authorized = "authorized",
}

export enum ManagerContext {
    start = "start", 
    base = "base",
    indexes = "indexes",
    formula = "formula",
    correction = "correction"
}

